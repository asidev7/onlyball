{
  "name": "onlyball-onchain",
  "version": "1.0.0",
  "private": true,
  "description": "OnlyBall lottery smart contract (TRON / TVM) + deployment",
  "scripts": {
    "compile": "tronbox compile",
    "deploy:shasta": "tronbox migrate --reset --network shasta",
    "deploy:mainnet": "tronbox migrate --reset --network mainnet",
    "console:mainnet": "tronbox console --network mainnet"
  },
  "devDependencies": {
    "tronbox": "^4.4.0"
  }
}
